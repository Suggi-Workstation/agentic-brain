# AGENTS.md - hub-brain

## Role
Shared brain. Highest-care repo in the org.

## Boundaries
- ALWAYS: follow `brain/library/CONVENTIONS.md` exactly; update the
  domain `_index.md` in the same commit as any topic change; keep
  front matter complete and descriptions dense.
- ASK FIRST: before creating a new domain or splitting a group.
- NEVER: edit anything in `brain/maxim/` (propose changes to Suggi
  instead); modify `.github/`; commit non-ASCII characters.

## Workspace hygiene
- Temp files go in `.agent_temp/` only. Delete them when done.
- If a command fails 3 times in a row: STOP, show the exact error, ask Suggi.
